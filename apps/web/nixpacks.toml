[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "PORT=$PORT npm start"

[healthcheck]
path = "/api/health"
port = "$PORT"
timeout = 30
interval = 10
retries = 3
